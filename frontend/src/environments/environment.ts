export const environment = {
  production: false,
  // When deploying to production on Render, replace this with your actual Render URL
  // e.g. apiUrl: 'https://kerya-api.onrender.com'
  apiUrl: 'http://127.0.0.1:8000'
};
